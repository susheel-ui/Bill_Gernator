<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Bill_Genrating_App" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="fontFamily">@font/poppins_medium</item>
    </style>

    <style name="Theme.Bill_Genrating_App" parent="Base.Theme.Bill_Genrating_App" />

<!--    button style-->
    <style name="cardBtn" parent="Base.Theme.Bill_Genrating_App">
        <item name="android:textSize">30dp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">10dp</item>
    </style>
<!--    radio Button style-->
    <style name="radioBtn">
        <item name="android:layout_marginLeft">10dp</item>
    </style>
    <style name="circleFAB">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
        <item name="backgroundColor">@color/primaryColor</item>
    </style>
    <style name="parentTop">
        <item name="layout_constraintStart_toStartOf">
            parent
        </item>
        <item name="layout_constraintEnd_toEndOf">parent</item>
        <item name="layout_constraintTop_toTopOf">parent</item>
        <item name="layout_constraintBottom_toBottomOf">parent</item>

    </style>
</resources>